{
"version": "0.2.0",
"configurations": [
  {
    "name": "Depurar C",
    "type": "cppdbg",
    "request": "launch",
    "program": "${workspaceFolder}/cub3D",  // Ruta a tu binario
    "args": ["assets/scenes/example.cub"],
    "stopAtEntry": false,
    "cwd": "${workspaceFolder}",
    "environment": [],
    "externalConsole": false,
    "MIMode": "gdb",
    "setupCommands": [
      {
        "description": "Habilitar impresión formateada para gdb",
        "text": "-enable-pretty-printing",
        "ignoreFailures": true
      }
    ]
  }
]
}